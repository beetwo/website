---
title: BeeTwo | Where we made a difference
pageable: true
per_page: 10
---


#news.menu-offset
  .ui.text.container
    .ui.divided.items
      - page_articles.each_with_index do |article, i|
        .ui.item
          .content
            .meta= article.date.strftime('%b %e')
            - if article.title
              .header= article.title
            .description= article.body